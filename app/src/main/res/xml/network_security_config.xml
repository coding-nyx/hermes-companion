<?xml version="1.0" encoding="utf-8"?>
<!--
  PoC scope: permit cleartext HTTP only to local-dev and Tailscale CGNAT
  ranges. Real Hermes gateways will be HTTPS; tighten or remove this
  config before any non-PoC build ships.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Tailscale CGNAT IPs (100.88.4.63 hub-11, 100.83.141.111 mac-mini) removed from
             the install-time cleartext allowlist. Tailscale classification is now handled
             at runtime by transport/discovery/TransportTiers.kt (100.64.0.0/10 + *.ts.net).
             Do not re-add here; the TransportTiers check is the right place. -->
    </domain-config>
</network-security-config>
